# Frontend

Minimal Vue 3 SPA for Strumok.

## Setup

Run from the `frontend/` folder:

```bash
bun install
```

## Run

Run from the `frontend/` folder:

```bash
bun run dev
```

Default app URL: `http://localhost:5173`

## Build / Preview

```bash
bun run build
bun run preview
```
